# 🎨 ВИЗУАЛЬНЫЕ ЭФФЕКТЫ - ИНСТРУКЦИЯ

## ⚠️ ВАЖНО: Временные эффекты для демонстрации!

Эти эффекты добавлены для красоты и легко удаляются.

---

## 🎯 Что добавлено:

### 1. **Анимация найденного пути** ✨
Когда находится путь, планеты подсвечиваются постепенно, одна за другой.

### 2. **Цветовая градация планет** 🎨
Цвет планеты зависит от количества связей:
- 🔵 **Синий** - мало связей (периферия)
- 🟢 **Зеленый** - среднее количество
- 🟡 **Желтый** - много связей
- 🔴 **Красный** - очень много (центральный хаб)

### 3. **Метеориты на фоне** ☄️
15 метеоритов летают на фоне с разной скоростью и светятся.

---

## 🔧 Как ВЫКЛЮЧИТЬ эффекты (перед сдачей лабы):

### Вариант 1: Выключить ВСЕ эффекты сразу

Открой файл: `include/ui/visual_effects.h`

Найди строку:
```cpp
constexpr bool ENABLE_VISUAL_EFFECTS = true;
```

Измени на:
```cpp
constexpr bool ENABLE_VISUAL_EFFECTS = false;
```

Пересобери проект:
```bash
make clean && make
```

**Готово!** Все эффекты выключены! ✅

---

### Вариант 2: Выключить отдельные эффекты

В том же файле `visual_effects.h` измени нужные:

```cpp
//выключаем анимацию, но оставляем метеориты
constexpr bool ENABLE_PATH_ANIMATION = false;      //выключено
constexpr bool ENABLE_PLANET_GRADATION = true;     //включено
constexpr bool ENABLE_METEORS = true;              //включено
```

---

## ⚙️ Настройка эффектов

В файле `visual_effects.h` можно менять параметры:

```cpp
//анимация пути
constexpr int PATH_ANIMATION_DELAY_MS = 300;      //скорость анимации
constexpr int PATH_GLOW_DURATION_MS = 500;        //длительность свечения

//градация планет
constexpr int MAX_CONNECTIONS_RED = 8;            //порог для красного цвета

//метеориты
constexpr int METEOR_COUNT = 15;                  //количество метеоритов
constexpr int METEOR_MIN_SPEED = 1;               //минимальная скорость
constexpr int METEOR_MAX_SPEED = 3;               //максимальная скорость
constexpr int METEOR_UPDATE_INTERVAL_MS = 50;     //частота обновления
```

---

## 📁 Какие файлы изменены:

```
include/ui/visual_effects.h    ← ГЛАВНЫЙ ФАЙЛ (включить/выключить)
include/ui/graphview.h          ← добавлены структуры
src/ui/graphview.cpp            ← добавлены эффекты
```

---

## 🗑️ Как ПОЛНОСТЬЮ удалить код эффектов:

Если нужно удалить не только выключить, а удалить весь код:

1. Удали файл: `include/ui/visual_effects.h`
2. В `graphview.h` удали:
   - `#include "visual_effects.h"`
   - `struct Meteor`
   - `QTimer* animationTimer;`
   - `QTimer* meteorTimer;`
   - `QVector<Meteor> meteors;`
   - методы `onAnimationStep()`, `onMeteorUpdate()`, `initMeteors()`, и т.д.

3. В `graphview.cpp` удали всё между комментариями:
   ```cpp
   //═══════════════════════════════════════════════════════════════
   // ВИЗУАЛЬНЫЕ ЭФФЕКТЫ (легко удалить изменив visual_effects.h)
   //═══════════════════════════════════════════════════════════════
   
   ... код эффектов ...
   
   //═══════════════════════════════════════════════════════════════
   // КОНЕЦ ВИЗУАЛЬНЫХ ЭФФЕКТОВ
   //═══════════════════════════════════════════════════════════════
   ```

Но проще просто **выключить** через `ENABLE_VISUAL_EFFECTS = false`! 😉

---

## ✅ Рекомендация:

**Для демонстрации:** `ENABLE_VISUAL_EFFECTS = true` 🎉
**Для сдачи лабы:** `ENABLE_VISUAL_EFFECTS = false` 📝

Одна строчка - и готово!

