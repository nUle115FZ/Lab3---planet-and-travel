# Руководство по использованию Qt приложения

## Быстрый старт

### 1. Открытие проекта в Qt Creator

```bash
# Запустите Qt Creator и откройте файл
CosmicLogistics.pro
```

### 2. Настройка проекта

1. При первом открытии Qt Creator предложит настроить проект
2. Выберите нужные компиляторы (Desktop Qt 5.15.x или Qt 6.x)
3. Нажмите "Configure Project"

### 3. Сборка

```
Меню Build → Build Project "CosmicLogistics"
или нажмите Ctrl+B
```

### 4. Запуск

```
Меню Build → Run
или нажмите Ctrl+R
```

## Возможные проблемы и решения

### Проблема 1: Qt не найден

**Решение:**
```bash
# macOS с Homebrew
brew install qt@5

# Добавьте в PATH
export PATH="/usr/local/opt/qt@5/bin:$PATH"

# Проверьте установку
qmake --version
```

### Проблема 2: Ошибка при сборке UI файлов

**Решение:**
Убедитесь, что установлены Qt Designer и uic:
```bash
which uic
# Должно показать путь к uic
```

### Проблема 3: Не собирается из-за C++17

**Решение:**
Проверьте CosmicLogistics.pro, должна быть строка:
```qmake
CONFIG += c++17
```

## Структура классов

### Иерархия наследования

```
Sequence<T> (абстрактный)
    ↓
DynamicArray<T>
    ↓
PriorityQueue<T, K> (приватное наследование)
```

### Основные методы

#### Sequence<T>
- `Get(int index)` - получить элемент
- `GetSize()` - размер
- `Append(item)` - добавить в конец
- `Prepend(item)` - добавить в начало
- `InsertAt(item, index)` - вставить по индексу

#### PriorityQueue<T, K>
- `Enqueue(item, priority)` - добавить с приоритетом
- `Dequeue()` - извлечь элемент с наивысшим приоритетом
- `Peek()` - посмотреть без извлечения
- `IsEmpty()` - проверка на пустоту

#### StarGraph
- `AddVertex(name)` - добавить планету
- `AddEdge(from, to, weight)` - добавить переход
- `GetEdges(vertex)` - получить смежные вершины
- `GetVertexCount()` - количество планет

#### Dijkstra
- `FindShortestPath(start, end)` - найти кратчайший путь
- `FindAllShortestPaths(start)` - все пути от вершины

## Примеры программирования

### Пример 1: Создание графа программно

```cpp
StarGraph graph;

// Добавляем планеты
int earth = graph.AddVertex("Земля");
int mars = graph.AddVertex("Марс");
int jupiter = graph.AddVertex("Юпитер");

// Добавляем переходы
graph.AddEdge(earth, mars, EdgeWeight(50.0, 0.1));
graph.AddEdge(mars, jupiter, EdgeWeight(100.0, 0.2));

// Поиск пути
Dijkstra dijkstra(graph);
DijkstraResult result = dijkstra.FindShortestPath(earth, jupiter);

if (result.pathExists) {
    std::cout << "Стоимость: " << result.totalCost << std::endl;
}
```

### Пример 2: Использование PriorityQueue

```cpp
PriorityQueue<std::string, double> pq;

// Добавляем элементы с приоритетами
pq.Enqueue("Низкий приоритет", 10.0);
pq.Enqueue("Высокий приоритет", 1.0);
pq.Enqueue("Средний приоритет", 5.0);

// Извлекаем в порядке приоритета
while (!pq.IsEmpty()) {
    std::string item = pq.Dequeue();
    std::cout << item << std::endl;
}
// Выведет: Высокий, Средний, Низкий
```

### Пример 3: Тест производительности

```cpp
Analytics analytics;

DynamicArray<int> sizes;
sizes.Append(10);
sizes.Append(50);
sizes.Append(100);

auto results = analytics.runPerformanceTests(sizes);
analytics.exportToCSV(results, "results.csv");
```

## Кастомизация интерфейса

### Изменение цветов графа

Откройте `graphview.cpp` и измените:

```cpp
// Цвет фона (космос)
gradient.setColorAt(0, QColor(10, 10, 30));  // Темнее
gradient.setColorAt(1, QColor(20, 20, 50));  // Светлее

// Цвет планет
gradient.setColorAt(0, QColor(255, 100, 100));  // Красный оттенок
```

### Изменение размера узлов

```cpp
// В graphview.cpp
const int radius = 40;  // Было 30, стало 40
```

### Добавление своих планет в демо-граф

Откройте `Analytics.h`, метод `generateDemoGraph()`:

```cpp
graph.AddVertex("Моя Планета");
graph.AddEdge("Терра", "Моя Планета", EdgeWeight(300.0, 0.15));
```

## Расширения проекта (идеи)

### 1. Добавить сохранение/загрузку графа

```cpp
// В StarGraph.h добавить методы:
void SaveToFile(const std::string& filename);
void LoadFromFile(const std::string& filename);
```

### 2. Реализовать другие алгоритмы

- Алгоритм Беллмана-Форда (для отрицательных весов)
- Алгоритм Флойда-Уоршелла (все пары кратчайших путей)
- Алгоритм A* (эвристический поиск)

### 3. Добавить анимацию поиска

```cpp
// В Dijkstra добавить callback для визуализации
std::function<void(int currentNode)> onNodeVisited;
```

### 4. Поддержка неориентированных графов

```cpp
void AddEdge(int from, int to, EdgeWeight weight, bool bidirectional = false) {
    adjacencyList[from].Append(Edge(to, weight));
    if (bidirectional) {
        adjacencyList[to].Append(Edge(from, weight));
    }
}
```

## Дополнительные ресурсы

### Документация Qt
- https://doc.qt.io/qt-5/
- https://doc.qt.io/qt-6/

### Алгоритмы на графах
- Кормен и др. "Алгоритмы: построение и анализ"
- https://cp-algorithms.com/

### Визуализация графов
- Graphviz
- Gephi

## Контрольный список перед сдачей

- [ ] Проект компилируется без ошибок
- [ ] Все тесты проходят успешно
- [ ] Интерфейс работает корректно
- [ ] README.md заполнен
- [ ] Код прокомментирован
- [ ] Демонстрационный граф загружается
- [ ] Поиск пути работает
- [ ] Тест производительности выполняется
- [ ] CSV экспорт работает
- [ ] Визуализация корректна
- [ ] Перемещение узлов работает

## Вопросы для защиты

1. **Какая сложность алгоритма Дейкстры?**
   - O((V + E) log V) с использованием PriorityQueue

2. **Почему PriorityQueue наследуется приватно?**
   - Чтобы скрыть методы базового класса Sequence, оставив только методы очереди

3. **Как работает min-heap?**
   - Бинарное дерево, где каждый родитель меньше своих детей

4. **Почему используется список смежности?**
   - Эффективнее для разреженных графов, O(V + E) памяти

5. **Что такое релаксация ребра?**
   - Обновление кратчайшего пути, если найден более короткий

---

**Успехов в защите!** 🎓

