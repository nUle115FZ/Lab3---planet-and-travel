# 🧪 Тесты для StarGraph

## Описание

Набор unit-тестов для проверки базового функционала класса `StarGraph`.

## Покрытие тестами

### ✅ Добавление вершин (4 теста)
- Добавление одной вершины
- Добавление нескольких вершин
- Обработка дубликатов имен
- Получение индекса вершины

### ✅ Добавление ребер (5 тестов)
- Добавление одного ребра
- Добавление нескольких ребер
- Добавление ребра по имени
- Обработка несуществующих вершин (по индексу)
- Обработка несуществующих вершин (по имени)

### ✅ Удаление вершин (6 тестов)
- Удаление одной вершины
- Удаление по имени
- Удаление вершины с входящими ребрами
- Удаление вершины с исходящими ребрами
- Обработка несуществующих вершин

### ✅ Удаление ребер (4 теста)
- Удаление одного ребра
- Удаление ребра по имени
- Удаление одного из нескольких ребер
- Обработка несуществующих ребер

### ✅ EdgeData (4 теста)
- Расчет общей стоимости
- Стоимость с нулевым риском
- Стоимость с высоким риском
- Сравнение

### ✅ PlanetData (2 теста)
- Конструктор
- Получение данных планеты

### ✅ Clear (2 теста)
- Очистка пустого графа
- Очистка непустого графа

### ✅ Комплексные тесты (2 теста)
- Множественные операции добавления/удаления
- Работа с циклами в графе

## Как запустить

```bash
cd tests
make run
```

## Структура

- `test_framework.h` - простой тестовый фреймворк (без зависимостей)
- `test_graph.cpp` - тесты для `StarGraph`
- `Makefile` - сборка тестов

## Результаты

```
🧪 Запуск тестов...

================================
✅ Пройдено: 28
❌ Провалено: 0
📊 Всего: 28

🎉 Все тесты пройдены!
```

## Макросы для тестирования

```cpp
TEST(name)                  // Объявление теста
ASSERT_TRUE(condition)       // Проверка истинности
ASSERT_FALSE(condition)      // Проверка ложности
ASSERT_EQUAL(a, b)          // Проверка равенства
ASSERT_THROWS(statement)    // Проверка что выбрасывается исключение
```

## Добавление новых тестов

```cpp
TEST(MyNewTest) {
    StarGraph graph;
    graph.AddVertex("Test");
    ASSERT_EQUAL(graph.GetVertexCount(), 1);
}
```

Тест автоматически зарегистрируется и будет запущен.


